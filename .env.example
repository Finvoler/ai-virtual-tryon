# ----------------------------------------------------------------------
# Replicate API
# ----------------------------------------------------------------------
# Create a token at https://replicate.com/account/api-tokens and paste it
# here. NEVER commit the real value — this file is .env.example only.
REPLICATE_API_TOKEN=r8_your_real_token_here

# Optional — override the model version pinned in app/api/clothes-swap/route.ts
# NEXT_PUBLIC_APP_URL=http://localhost:3000
